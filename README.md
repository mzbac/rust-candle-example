# Misral 7b Rust Candle Example

a simplified Rust candle example for running the Misral 7b model. 


## Usage

- Download the model weights and tokenizer.json into the models folder.
- Run `cargo run --release`.
